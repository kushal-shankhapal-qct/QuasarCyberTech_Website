// Header.tsx — Legacy re-export
// All pages import from './Header'. This file now delegates to the new Navbar component.
// The old Header implementation is preserved in git history.

export { default } from './Navbar';
